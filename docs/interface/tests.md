# Platform Interface Tests

## Coverage

The platform interface package is covered by Flutter-side unit tests.

These tests validate:

- registration behavior
- fail-fast behavior when no implementation is present
- platform interface expectations used by the app-facing package

## Command

```sh
cd flutter_badge_manager_platform_interface && make test-unit
```
